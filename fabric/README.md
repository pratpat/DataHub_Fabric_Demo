# Fabric assets

- `lakehouses/` — Bronze / Silver layouts (folder structure, table list).
- `notebooks/` — PySpark for ingest, normalize, reconcile, generate samples.
- `pipelines/` — Data Factory pipeline JSON.
